# 学院案例详情页开发说明

## 概述

已完成学院案例详情页面的开发，基于 Figma 设计稿（node-id=566-3541）实现。

## 文件位置

- **详情页组件**: `src/views/College/Detail.vue`
- **路由配置**: `src/router/modules/public.ts` (已存在)
- **API 接口**: `src/api/college.ts` (已存在)

## 功能特性

### 1. 页面结构

页面包含以下主要部分：

1. **页头导航**
   - 使用 `AppHeader` 组件
   - 使用 `AppNavigation` 组件

2. **Banner 区域**
   - 显示"一院一品"标题
   - 渐变文字效果
   - 背景图片：`/images/Frame 427321206.png`

3. **面包屑导航**
   - 显示路径：首页 >> 一院一品 >> 作品详情
   - 支持点击跳转

4. **作品信息卡片**
   - 左侧：封面图片（540x304px）
   - 右侧信息：
     - 作品标题
     - 所属学院
     - 学院介绍（带红色标识条）
     - 累计观看人数统计

5. **内容区域**
   - **左侧垂直导航**：
     - 作品视频（默认选中）
     - 图文介绍（待定）
   - **右侧内容区**：
     - 视频播放器（514px 高度）
     - 视频列表（横向滚动）
     - 图文介绍区域（开发中）

6. **相关推荐**
   - 展示 4 个推荐的学院案例卡片
   - 自动过滤当前案例
   - 支持点击跳转到其他案例详情

7. **页脚**
   - 使用 `PageFooter` 组件

### 2. 核心功能

#### 数据加载
```typescript
// 自动根据路由参数加载详情
const id = Number(route.params.id)
const detail = await getCollegeDetail(id)
```

#### 视频播放
- 使用 `VideoPlayer` 组件播放视频
- 支持视频列表切换（预留接口）
- 当前主视频从 `detail.videoUrl` 加载

#### Tab 切换
- 作品视频 / 图文介绍 切换
- 左侧导航高亮显示当前选中项

#### 相关推荐
- 自动加载 4 条推荐案例
- 点击推荐卡片跳转并重新加载详情

### 3. 样式设计

#### 颜色规范
- 主色调红色：`#c30d23`
- 基础黑色：`#333333`
- 背景灰色：`#f9f9f9`

#### 布局
- 最大内容宽度：`1200px`
- 主内容居中布局
- 响应式设计支持移动端

#### 交互效果
- 卡片 hover 上浮效果
- 视频缩略图 hover 显示播放按钮
- 平滑滚动过渡

### 4. 响应式设计

```css
/* 平板设备 */
@media (max-width: 1200px) {
  - 推荐卡片：4列 → 3列
  - 详情卡片：横向 → 纵向堆叠
}

/* 移动设备 */
@media (max-width: 768px) {
  - 推荐卡片：3列 → 2列
  - 左侧导航：纵向 → 横向
  - Banner 标题缩小
}

/* 小屏手机 */
@media (max-width: 480px) {
  - 推荐卡片：2列 → 1列
}
```

## 使用方法

### 访问详情页

1. **从列表页跳转**：
```vue
// 在 src/views/College/index.vue 中
<div @click="goToDetail(item.id)">
  <!-- 卡片内容 -->
</div>
```

2. **直接访问**：
```
http://localhost:5173/college/detail/1
```

### 路由配置

路由已在 `src/router/modules/public.ts` 中配置：
```typescript
{
  path: '/college/detail/:id',
  name: 'CollegeDetail',
  component: () => import('@/views/College/Detail.vue'),
  meta: {
    title: '学院案例详情',
    requiresAuth: false
  }
}
```

## API 接口

### 获取详情
```typescript
import { getCollegeDetail } from '@/api/college'

const detail = await getCollegeDetail(id)
```

### 响应数据结构
```typescript
interface CollegeItem {
  id: number              // 业务id
  name: string           // 课程名称
  coverUrl: string       // 封面图片
  college: string        // 所在学院
  teachers: Teacher[]    // 主讲教师
  types: string[]        // 建设课程类型
  content: string        // 课程详细内容
  videoUrl?: string      // 视频地址
  statPv: number         // 浏览量
  showFront: number      // 是否前台显示
  createTime: string     // 发布时间
}
```

## 待完善功能

### 1. 视频列表
当前视频列表使用模拟数据，需要：
- 后端提供视频列表接口
- 更新视频数据结构
- 实现真实的视频切换功能

```typescript
// 当前模拟数据
const videoList = ref([
  { name: '视频名称', url: '', thumbnail: '' },
  // ...更多视频
])

// 需要实现的接口
interface Video {
  id: number
  name: string
  url: string
  thumbnail: string
  duration?: string
}
```

### 2. 图文介绍
"图文介绍"Tab 当前仅显示占位符，需要：
- 确定图文内容的数据结构
- 实现富文本内容展示
- 可能需要图片画廊功能

### 3. 标签徽章
设计稿中有"国家示范"等标签，当前隐藏（`opacity: 0`），需要：
- 后端提供标签数据
- 根据实际需求显示标签

## 注意事项

### 1. 图片资源
确保以下图片存在于 `public/images/` 目录：
- `Frame 427321206.png` - Banner 背景图
- `image.png` - 标题装饰线图案

### 2. 视频播放器
视频播放器组件位于：
```
src/components/common/VideoPlayer/index.vue
```

使用时需要提供有效的视频 URL：
```vue
<VideoPlayer
  :src="videoUrl"
  :poster="coverUrl"
/>
```

### 3. 浏览量统计
如果需要统计页面浏览量，可在 `onMounted` 钩子中添加：
```typescript
// 记录浏览量
const recordView = async () => {
  try {
    await request.post(`/api/special/college/view/${id}`)
  } catch (error) {
    console.error('记录浏览量失败', error)
  }
}
```

## 测试建议

### 1. 功能测试
- [ ] 页面正常加载详情数据
- [ ] 面包屑导航跳转正常
- [ ] 视频播放器正常工作
- [ ] Tab 切换正常
- [ ] 推荐卡片点击跳转正常
- [ ] 响应式布局在不同屏幕尺寸下正常

### 2. 异常测试
- [ ] 无效 ID 参数处理
- [ ] 详情加载失败处理
- [ ] 无视频时的占位显示
- [ ] 网络错误提示

### 3. 性能测试
- [ ] 图片懒加载（如需要）
- [ ] 大量推荐卡片的渲染性能
- [ ] 视频预加载策略

## 参考资料

- **Figma 设计稿**: [福州大学思政官网 - 一院一品详情](https://www.figma.com/design/61VdOb1lIMdp25yHxAygzx/%E7%A6%8F%E5%B7%9E%E5%A4%A7%E5%AD%A6%E6%80%9D%E6%94%BF%E5%AE%98%E7%BD%91?node-id=566-3541&m=dev)
- **列表页实现**: `src/views/College/index.vue`
- **API 文档**: `src/api/college.ts`

## 更新日志

### 2026-01-16
- ✅ 完成详情页面基础框架
- ✅ 实现详情数据加载
- ✅ 实现视频播放功能
- ✅ 实现相关推荐模块
- ✅ 实现响应式布局
- ⏳ 视频列表待完善（需要后端接口）
- ⏳ 图文介绍待实现

---

如有问题或需要调整，请随时反馈！

