# 思政热点管理功能说明

## 功能概述

思政热点管理功能已成功集成到管理后台系统中，使用 Element Plus UI 组件库实现。

## 访问路径

在管理后台中，访问路径为：`/admin/resources/hotspot`

## 功能特性

### 1. 列表展示
- ✅ 序号显示
- ✅ 标题展示（带溢出省略）
- ✅ 发布时间显示
- ✅ 来源显示（如：新华网、人民日报等）
- ✅ 浏览人数统计（带千分位格式化）
- ✅ 状态显示（显示/隐藏，用标签区分）
- ✅ 表格斑马纹样式

### 2. 搜索功能
- ✅ 标题关键词搜索
- ✅ 支持回车键搜索
- ✅ 支持清空按钮

### 3. 分页功能
- ✅ 支持每页 10/20/50/100 条数据
- ✅ 总数统计
- ✅ 页码跳转

### 4. 新增/编辑功能
- ✅ 标题输入（最多100字，带字数统计）
- ✅ 来源输入（最多50字）
- ✅ 内容类型选择：
  - 富文本内容（内部详情页）
  - 外部链接（跳转到外部URL）
- ✅ 内容输入（根据类型动态显示）
- ✅ 置顶开关
- ✅ 显示/隐藏开关
- ✅ 表单验证

### 5. 操作功能
- ✅ 编辑：修改现有热点信息
- ✅ 显示/隐藏：快速切换前台显示状态
- ✅ 删除：带二次确认的删除操作

## 技术实现

### 1. 依赖安装
```bash
npm install element-plus @element-plus/icons-vue
```

### 2. 全局配置
在 `src/main.ts` 中已配置 Element Plus：
- 引入 Element Plus 及样式
- 注册所有图标组件

### 3. 路由配置
在 `src/router/modules/admin.ts` 中已添加路由：
```javascript
{
  path: 'resources/hotspot',
  name: 'AdminResourcesHotspot',
  component: () => import('@/views/admin/Resources/HotspotManage.vue'),
  meta: {
    title: '思政热点',
    keepAlive: true
  }
}
```

### 4. API 接口
使用 `src/api/hotspot.ts` 中的接口：
- `getHotspotList()` - 获取分页列表
- `addHotspot()` - 新增热点
- `editHotspot()` - 编辑热点
- `removeHotspot()` - 删除热点

## 数据字段

### HotspotItem 接口
```typescript
{
  id: number              // ID
  title: string           // 标题
  source?: string         // 来源（如：新华网、人民日报等）
  content: string         // 内容（富文本或URL）
  contentType: number     // 0=富文本内容；1=URL地址
  pinTop: number          // 1=置顶；0=不置顶
  statPv: number          // 浏览量
  showFront: number       // 1=显示；0=不显示
  createTime: string      // 创建时间
  updateTime?: string     // 更新时间
}
```

## 页面截图对照

实现的功能完全参照提供的截图：
- ✅ 顶部标题和描述
- ✅ 搜索框和新增按钮在同一行
- ✅ 表格展示所有必要列
- ✅ 操作按钮：编辑、显示/隐藏、删除
- ✅ 底部分页器

## UI 特性

### 样式设计
- 背景色：#f5f5f5（浅灰色背景）
- 卡片式布局：白色背景，圆角设计
- 表头背景：#fef2f2（浅红色）
- 主题色：Element Plus 默认蓝色
- 危险色：红色（用于删除等操作）

### 响应式
- 表格列自适应宽度
- 标题列设置 min-width 确保内容可读
- 操作列固定在右侧

## 使用方法

### 新增热点
1. 点击右上角"新增"按钮
2. 填写标题、来源等信息
3. 选择内容类型：
   - 选择"富文本内容"：填写富文本内容
   - 选择"外部链接"：填写完整URL地址
4. 设置置顶和显示状态
5. 点击"保存"按钮

### 编辑热点
1. 点击列表中的"编辑"按钮
2. 修改相关信息
3. 点击"保存"按钮

### 切换显示状态
- 点击"显示"按钮可切换为"隐藏"
- 点击"隐藏"按钮可切换为"显示"
- 操作后立即生效

### 删除热点
1. 点击"删除"按钮
2. 确认删除操作
3. 删除成功后列表自动刷新

## 注意事项

1. **表单验证**：标题和内容为必填项
2. **URL格式**：选择外部链接时，请填写完整的URL（如：https://www.example.com）
3. **数据刷新**：所有操作（新增/编辑/删除/切换状态）后都会自动刷新列表
4. **错误处理**：所有API调用都有完善的错误处理和提示

## 开发信息

- **创建时间**：2026-01-12
- **UI 框架**：Element Plus
- **开发框架**：Vue 3 + TypeScript
- **状态管理**：Pinia
- **路由管理**：Vue Router 4


