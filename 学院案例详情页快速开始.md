# 学院案例详情页快速开始

## 访问详情页

### 方式一：从列表页跳转

1. 启动开发服务器：
```bash
npm run dev
```

2. 访问学院列表页：
```
http://localhost:5173/college
```

3. 点击任意学院案例卡片，自动跳转到详情页

### 方式二：直接访问

直接在浏览器中访问详情页 URL：
```
http://localhost:5173/college/detail/1
```

> 注意：将 URL 中的 `1` 替换为实际的学院案例 ID

## 页面功能测试

### 1. 基础信息展示
- [ ] 页面正常加载
- [ ] 显示学院案例标题
- [ ] 显示所属学院
- [ ] 显示学院介绍
- [ ] 显示累计观看人数

### 2. 面包屑导航
- [ ] 点击"首页"跳转到首页
- [ ] 点击"一院一品"跳转到列表页

### 3. Tab 切换
- [ ] 点击"作品视频" Tab（默认选中）
- [ ] 点击"图文介绍" Tab（显示开发中提示）
- [ ] 左侧导航高亮正确

### 4. 视频播放
- [ ] 如果有视频，视频播放器正常显示
- [ ] 视频可以正常播放/暂停
- [ ] 如果没有视频，显示"暂无视频"提示

### 5. 相关推荐
- [ ] 底部显示推荐的学院案例卡片
- [ ] 点击推荐卡片跳转到对应详情页
- [ ] 页面刷新并滚动到顶部

### 6. 响应式布局
- [ ] 桌面端（>1200px）：推荐卡片 4 列
- [ ] 平板端（768px-1200px）：推荐卡片 3 列
- [ ] 移动端（<768px）：推荐卡片 2 列
- [ ] 小屏手机（<480px）：推荐卡片 1 列

## 常见问题

### Q1: 页面显示"加载失败"

**原因**：后端 API 未启动或网络错误

**解决方案**：
1. 确认后端服务已启动
2. 检查 API 地址配置是否正确
3. 查看浏览器控制台的错误信息

### Q2: 视频无法播放

**原因**：
- 视频 URL 不存在或无效
- 视频格式浏览器不支持
- 跨域问题

**解决方案**：
1. 检查 `detail.videoUrl` 是否有值
2. 确认视频 URL 可访问
3. 使用支持的视频格式（MP4, WebM, Ogg）

### Q3: 推荐卡片不显示

**原因**：
- 数据库中案例数量不足
- API 返回数据为空

**解决方案**：
1. 检查数据库中是否有足够的案例数据
2. 确认 `showFront` 字段设置为 1
3. 查看控制台日志

### Q4: 面包屑导航跳转失败

**原因**：路由配置问题

**解决方案**：
1. 确认路由配置正确
2. 检查 `vue-router` 是否正常工作
3. 查看浏览器控制台的路由错误

## 开发调试

### 启用调试日志

在组件中已包含调试日志：
```typescript
console.log('加载详情失败:', error)
console.log('加载推荐列表失败:', error)
```

打开浏览器开发者工具的 Console 面板查看。

### 检查 API 请求

1. 打开浏览器开发者工具
2. 切换到 Network 面板
3. 刷新页面
4. 查看以下请求：
   - `/api/special/college/detail/{id}` - 详情数据
   - `/api/special/college/page/list` - 推荐列表

### Mock 数据测试

如果后端 API 未准备好，可以临时使用 Mock 数据：

```typescript
// 在 loadDetail 函数中临时使用 Mock 数据
const loadDetail = async () => {
  loading.value = true
  try {
    // Mock 数据
    detail.value = {
      id: 1,
      name: '交通运输系统导论',
      coverUrl: '/images/placeholder.jpg',
      college: '信息学院',
      teachers: [{ name: '张老师', title: '教授' }],
      types: ['课程思政'],
      content: '这是学院介绍的内容...',
      videoUrl: 'https://example.com/video.mp4',
      statPv: 17645,
      showFront: 1,
      createTime: '2026-01-07'
    }
    currentVideoUrl.value = detail.value.videoUrl || ''
    loadRecommendList()
  } finally {
    loading.value = false
  }
}
```

## 性能优化建议

### 1. 图片懒加载

对于推荐卡片的图片，可以使用懒加载：
```vue
<img 
  :src="item.coverUrl" 
  :alt="item.name" 
  loading="lazy"
/>
```

### 2. 视频预加载

根据需要设置视频预加载策略：
```vue
<VideoPlayer
  :src="currentVideoUrl"
  :poster="detail.coverUrl"
  preload="metadata"
/>
```

### 3. 组件缓存

在路由配置中启用 keep-alive：
```typescript
{
  path: '/college/detail/:id',
  name: 'CollegeDetail',
  component: () => import('@/views/College/Detail.vue'),
  meta: {
    title: '学院案例详情',
    requiresAuth: false,
    keepAlive: true  // 启用缓存
  }
}
```

## 下一步开发

- [ ] 实现视频列表功能（需要后端接口）
- [ ] 实现图文介绍模块
- [ ] 添加标签徽章功能
- [ ] 添加分享功能
- [ ] 添加收藏功能
- [ ] 优化 SEO
- [ ] 添加页面浏览量统计

## 相关文档

- [学院案例详情页开发说明](./学院案例详情页开发说明.md)
- [一院一品列表页说明](./一院一品页面实现说明.md)
- [API 接口文档](./API-测试说明.md)

---

如有问题，请查看开发说明文档或联系技术支持。

