# 后台管理系统部署说明

## 概述

本项目已配置为多页面应用（Multi-Page Application），支持前台和后台系统的独立访问入口。

## 访问地址

- **前台网站**: `https://dszk.fzu.edu.cn/`
- **后台管理系统**: `https://dszk.fzu.edu.cn/admin/index.html`

## 项目结构

```
zhihuishu-app/
├── index.html                 # 前台网站入口
├── admin/
│   └── index.html            # 后台管理系统入口
├── src/
│   ├── main.ts               # 前台入口文件
│   ├── admin.ts              # 后台入口文件
│   ├── views/
│   │   ├── portal/           # 前台页面
│   │   └── admin/            # 后台管理页面
│   └── layouts/
│       ├── PortalLayout.vue  # 前台布局
│       └── AdminLayout.vue   # 后台布局
```

## 构建配置

项目使用 Vite 的多页面应用配置（在 `vite.config.ts` 中）：

```typescript
build: {
  rollupOptions: {
    input: {
      main: resolve(__dirname, 'index.html'),
      admin: resolve(__dirname, 'admin/index.html')
    }
  }
}
```

## 开发环境

### 本地开发

```bash
npm run dev
```

在开发环境中：
- 前台网站: `http://localhost:5173/`
- 后台管理: `http://localhost:5173/admin/`

### 构建生产版本

```bash
npm run build
```

构建后的文件位于 `dist/` 目录：
```
dist/
├── index.html              # 前台网站
├── admin/
│   └── index.html         # 后台管理系统
└── assets/                # 共享资源文件
```

## 部署方式

### 方式一：Nginx 部署（推荐）

在 Nginx 配置中：

```nginx
server {
    listen 80;
    server_name dszk.fzu.edu.cn;

    root /path/to/dist;
    index index.html;

    # 前台网站
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后台管理系统
    location /admin {
        try_files $uri $uri/ /admin/index.html;
    }

    # 静态资源
    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 方式二：Apache 部署

在 `.htaccess` 文件中配置：

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # 后台管理系统路由
  RewriteRule ^admin/index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^admin/ /admin/index.html [L]
  
  # 前台网站路由
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

## 后台管理功能模块

后台管理系统包含以下功能模块：

1. **资源管理**
   - 首页Banner配置
   - 思政概览管理
   - 习思想伟大实践
   - 思政资源管理
   - 特色专题管理

2. **题库管理**
   - 题目录入和编辑
   - 题库分类管理

3. **数据分析与统计**
   - 访问量统计
   - 用户行为分析

4. **AI智能管理**
   - AI助手配置
   - 智能问答管理

5. **系统配置**
   - 系统参数设置
   - 用户权限管理

## 路由配置

后台管理系统的路由都以 `/admin` 开头：

- `/admin/resources/home` - 首页Banner配置
- `/admin/resources/party-course` - 党建+课程思政
- `/admin/resources/model-display` - 示范展播
- `/admin/resources/practice-achievement` - 习思想融入成果
- `/admin/resources/practice-video` - 习思想优秀视频展播
- `/admin/resources/practice-case` - 习思想融入学科教学实践
- `/admin/resources/practice-footprint` - 总书记的福建足迹
- `/admin/resources/list` - 思政资源
- `/admin/resources/excellent-class` - 优秀思政课堂
- `/admin/resources/college-special` - 一院一品
- `/admin/resources/culture-map` - 红色文化资源地图
- `/admin/questions` - 题库管理
- `/admin/analysis` - 数据分析
- `/admin/ai` - AI助手
- `/admin/system` - 系统配置

## 权限说明

后台管理系统部分功能需要登录认证：
- 资源管理模块：无需认证（`requiresAuth: false`）
- 题库管理：需要认证，角色：admin 或 teacher
- 数据分析：需要认证，角色：admin
- AI助手：需要认证，角色：admin 或 teacher
- 系统配置：无需认证

可在 `src/router/modules/admin.ts` 中修改权限配置。

## 测试验证

构建完成后，可以通过以下方式验证：

1. **本地预览**:
   ```bash
   npm run preview
   ```
   访问 `http://localhost:4173/admin/index.html`

2. **检查构建产物**:
   确认 `dist/admin/index.html` 文件存在

3. **部署后验证**:
   访问 `https://dszk.fzu.edu.cn/admin/index.html`

## 常见问题

### Q: 访问后台管理系统时出现 404 错误

A: 确保服务器配置正确处理了 `/admin` 路径的路由重写。

### Q: 静态资源加载失败

A: 检查 Nginx/Apache 的 root 路径配置是否正确指向 `dist` 目录。

### Q: 页面刷新后出现 404

A: 确保配置了路由的 `try_files` 或 `.htaccess` 重写规则。

## 维护建议

1. 定期备份后台管理系统的数据
2. 为后台管理系统配置独立的访问日志
3. 考虑为后台系统添加 IP 白名单限制
4. 建议使用 HTTPS 确保数据传输安全

## 技术支持

如有问题，请联系前端架构组。








