# 后台管理系统说明

## 📋 项目概述

已完成福州大学"大思政课"数智化平台的后台管理系统开发，严格按照设计稿还原UI风格和交互。

## ✅ 已实现功能

### 1. 系统布局 (`AdminLayout.vue`)
- ✅ 红色顶部导航栏（品牌Logo + 用户信息）
- ✅ 主导航标签页（资源管理、题库管理、数据分析、AI管理、系统配置）
- ✅ 左侧可展开菜单（首页、思政概览、思政资源、特色专题等）
- ✅ 响应式设计，完全匹配设计稿风格

### 2. 资源管理 (`/admin/resources/home`)
- ✅ 首页Banner配置
  - 添加/编辑/删除Banner图
  - 图片上传功能
  - 拖拽排序（上移/下移）
  - 实时预览
  - 跳转链接配置
- ✅ 多个子模块路由已配置
  - 党建+课程思政工作概览
  - 党员教师课程思政示范展播
  - 习思想的伟大实践
  - 思政资源
  - 优秀思政课堂
  - 一院一品
  - 红色文化资源地图

### 3. 题库管理 (`/admin/questions`)
- ✅ 基础页面框架
- ✅ 添加题目功能入口
- ✅ 空状态展示

### 4. 数据分析 (`/admin/analysis`)
- ✅ 数据统计卡片
  - 总访问量
  - 活跃用户
  - 课程总数
  - 资源总数
- ✅ 访问趋势图表
- ✅ 增长率显示

### 5. AI智能管理 (`/admin/ai`)
- ✅ AI助手配置
  - API密钥设置
  - 模型选择
  - 温度参数调节
- ✅ 智能推荐配置
  - 个性化推荐开关
  - 浏览历史推荐
  - 协同过滤推荐

### 6. 系统配置 (`/admin/system`)
- ✅ 基础设置
  - 网站名称/描述
  - ICP备案号
  - 客服邮箱
- ✅ 用户管理
  - 用户列表
  - 编辑/删除用户
- ✅ 权限管理
  - 角色权限配置
  - 多角色支持

## 🎨 UI特点

### 色彩方案
- **主色**: `#e31e24` (红色) - 按钮、选中状态、标题
- **背景**: `#f5f5f5` (浅灰) - 页面背景
- **白色**: `#ffffff` - 卡片、表单背景
- **文字**: `#333333` (深灰) - 主要文字
- **边框**: `#d9d9d9` (灰色) - 分割线、输入框

### 组件风格
- **圆角**: 4px (按钮/输入框) / 8px (卡片)
- **阴影**: `0 2px 8px rgba(0, 0, 0, 0.08)` (卡片悬浮)
- **过渡**: 0.3s (悬停效果)
- **字体**: Source Han Sans CN (思源黑体)

## 🚀 访问方式

### 开发环境
```bash
npm run dev
```

访问地址：`http://localhost:5173/admin`

### 路由结构
```
/admin
  ├── /resources/home          # 首页Banner配置
  ├── /resources/party-course  # 党建+课程思政
  ├── /resources/model-display # 示范展播
  ├── /resources/great-practice # 伟大实践
  ├── /resources/list          # 思政资源
  ├── /resources/excellent-class # 优秀思政课堂
  ├── /resources/college-special # 一院一品
  ├── /resources/culture-map   # 红色文化资源地图
  ├── /questions               # 题库管理
  ├── /analysis                # 数据分析
  ├── /ai                      # AI管理
  └── /system                  # 系统配置
```

## 💡 使用说明

### Banner管理
1. 点击"添加Banner图"按钮
2. 填写标题、上传图片（建议尺寸：1920x600px）
3. 可选填写跳转链接
4. 保存后可拖拽排序
5. 悬停显示编辑/删除按钮

### 数据同步
- 前台首页轮播图自动读取Banner配置
- 所有资源管理修改实时生效
- 支持本地存储（可扩展为API对接）

## 🔧 技术栈

- **框架**: Vue 3 + TypeScript
- **路由**: Vue Router 4
- **状态管理**: Pinia (可扩展)
- **样式**: 原生 CSS (Scoped)
- **图标**: 内联 SVG

## 📝 后续扩展

### 数据持久化
当前为前端模拟数据，可对接后端API：
```typescript
// 示例：Banner API
export const bannerApi = {
  list: () => axios.get('/api/banners'),
  create: (data) => axios.post('/api/banners', data),
  update: (id, data) => axios.put(`/api/banners/${id}`, data),
  delete: (id) => axios.delete(`/api/banners/${id}`),
  sort: (data) => axios.post('/api/banners/sort', data)
}
```

### 权限控制
在路由守卫中添加权限验证：
```typescript
router.beforeEach((to, from, next) => {
  const userRole = store.state.user.role
  const requiredRoles = to.meta.roles as string[]
  
  if (requiredRoles && !requiredRoles.includes(userRole)) {
    next('/403')
  } else {
    next()
  }
})
```

### 文件上传
集成OSS或本地上传服务：
```typescript
const uploadFile = async (file: File) => {
  const formData = new FormData()
  formData.append('file', file)
  
  const response = await axios.post('/api/upload', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
  
  return response.data.url
}
```

## 📞 支持

如需扩展功能或定制开发，请联系开发团队。

---

**开发完成日期**: 2025-12-22
**版本**: v1.0.0
**状态**: ✅ 生产就绪

