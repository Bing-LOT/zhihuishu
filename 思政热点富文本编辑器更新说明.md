# 思政热点富文本编辑器更新说明

## 📋 更新概述

根据新的 UI 设计，更新了思政热点新增/编辑表单，使用 wangEditor 富文本编辑器替代原有的简单文本框。

**更新日期**: 2026-01-12  
**版本**: v1.1.0

---

## ✨ 主要变更

### 1. 表单简化
**移除的字段**:
- ❌ 内容类型（富文本内容/外部链接）
- ❌ 是否置顶开关
- ❌ 是否显示开关

**保留的字段**:
- ✅ 标题（必填）
- ✅ 来源（选填）
- ✅ 热点内容（必填，使用富文本编辑器）

### 2. 富文本编辑器集成
- **编辑器**: wangEditor 5.x
- **组件库**: @wangeditor/editor-for-vue
- **高度**: 400px
- **模式**: default（完整功能）

### 3. 按钮文字调整
- "保存" → "提交"
- 保持"取消"按钮不变

---

## 🎨 富文本编辑器功能

### 工具栏功能
- **文本格式**：标题、加粗、斜体、下划线
- **颜色**：文字颜色、背景色
- **字体**：字号、字体、行高
- **列表**：无序列表、有序列表、待办事项
- **插入**：表情、链接、图片、视频、表格、代码块
- **操作**：撤销、重做、全屏

### 图片上传
当前使用 **base64** 方式临时处理图片：
- 支持直接插入图片
- 图片自动转换为 base64 编码
- **注意**: 生产环境建议实现服务器端上传

```typescript
// 自定义图片上传（当前实现）
uploadImage: {
  async customUpload(file: File, insertFn: any) {
    const reader = new FileReader()
    reader.onload = (e) => {
      const url = e.target?.result as string
      insertFn(url, '', '')
    }
    reader.readAsDataURL(file)
  }
}
```

---

## 📦 依赖信息

### 已有依赖（无需安装）
```json
{
  "@wangeditor/editor": "^5.1.23",
  "@wangeditor/editor-for-vue": "^5.1.12"
}
```

项目中已经安装了 wangEditor，无需额外安装。

---

## 🔧 技术实现

### 组件导入
```typescript
import { Editor, Toolbar } from '@wangeditor/editor-for-vue'
import '@wangeditor/editor/dist/css/style.css'
import type { IDomEditor, IEditorConfig, IToolbarConfig } from '@wangeditor/editor'
```

### 编辑器配置
```typescript
// 工具栏配置
const toolbarConfig: Partial<IToolbarConfig> = {
  toolbarKeys: [
    'headerSelect', 'bold', 'italic', 'underline',
    'color', 'bgColor', 'fontSize', 'fontFamily',
    'lineHeight', 'bulletedList', 'numberedList',
    'todo', 'emotion', 'insertLink', 'insertImage',
    'insertVideo', 'insertTable', 'codeBlock',
    'undo', 'redo', 'fullScreen'
  ]
}

// 编辑器配置
const editorConfig: Partial<IEditorConfig> = {
  placeholder: '请输入内容...'
}
```

### 生命周期管理
```typescript
// 编辑器创建
const handleCreated = (editor: IDomEditor) => {
  editorRef.value = editor
}

// 组件销毁时销毁编辑器
onBeforeUnmount(() => {
  const editor = editorRef.value
  if (editor) {
    editor.destroy()
  }
})

// 对话框关闭时清空编辑器
const handleDialogClose = () => {
  if (editorRef.value) {
    editorRef.value.clear()
  }
}
```

---

## 🎯 使用方法

### 新增热点
1. 点击"新增"按钮
2. 填写标题和来源
3. 在富文本编辑器中输入内容：
   - 可以设置文字格式
   - 可以插入图片、链接等
   - 支持复制粘贴
4. 点击"提交"按钮保存

### 编辑热点
1. 点击列表中的"编辑"按钮
2. 富文本编辑器自动加载原有内容
3. 修改内容后点击"提交"保存

---

## 📊 数据格式

### 保存的数据格式
```typescript
{
  title: string       // 标题
  source?: string     // 来源
  content: string     // HTML 格式的富文本内容
  contentType: 0      // 固定为 0（富文本）
  pinTop: 0          // 固定为 0（不置顶）
  showFront: 1       // 固定为 1（显示）
}
```

### 富文本内容示例
```html
<h1>这是标题</h1>
<p>这是一段<strong>加粗的文字</strong>和<em>斜体文字</em></p>
<ul>
  <li>列表项1</li>
  <li>列表项2</li>
</ul>
<p><img src="data:image/png;base64,..." alt="" /></p>
```

---

## 🔍 与前版本的差异

| 项目 | 版本 1.0.0 | 版本 1.1.0（当前）|
|------|--------|---------------|
| 内容编辑 | 简单文本框 | 富文本编辑器 |
| 内容类型 | 富文本/外部链接 | 仅富文本 |
| 置顶功能 | 可选 | 已移除 |
| 显示开关 | 可选 | 已移除（默认显示） |
| 对话框宽度 | 800 像素 | 900 像素 |
| 标签宽度 | 120 像素 | 100 像素 |
| 提交按钮 | "保存" | "提交" |

---

## ⚠️ 注意事项

### 1. 图片上传
**当前状态**: 使用 base64 编码
- ✅ 优点：无需服务器支持，即时可用
- ❌ 缺点：数据库体积大，传输慢

**建议改进**:
```typescript
// TODO: 实现服务器端图片上传
uploadImage: {
  server: '/api/upload/image',
  fieldName: 'file',
  maxFileSize: 5 * 1024 * 1024, // 5MB
  allowedFileTypes: ['image/*']
}
```

### 2. 内容验证
- 富文本内容保存为 HTML 格式
- 前端需要验证内容不为空
- 后端建议进行 XSS 过滤

### 3. 编辑器高度
- 当前高度：400px
- 可根据需要在 `.editor-content` 样式中调整

### 4. 工具栏定制
可根据需要调整 `toolbarConfig.toolbarKeys` 数组来定制工具栏按钮。

---

## 🐛 已知问题

### 问题1: 图片大小
**描述**: base64 编码会使图片体积变大约 33%  
**影响**: 数据库存储压力增加  
**解决**: 实现服务器端图片上传

### 问题2: 粘贴格式
**描述**: 从 Word 等软件粘贴可能带来冗余样式  
**影响**: 可能影响显示效果  
**解决**: 使用编辑器的"清除格式"功能

---

## 📚 参考文档

- [wangEditor 官方文档](https://www.wangeditor.com/)
- [wangEditor Vue3 文档](https://www.wangeditor.com/v5/for-frame.html#vue3)
- [API 文档](https://www.wangeditor.com/v5/API.html)

---

## 🔄 更新日志

### v1.1.0 (2026-01-12)
- ✅ 集成 wangEditor 富文本编辑器
- ✅ 简化表单字段（移除内容类型、置顶、显示开关）
- ✅ 更新对话框宽度为 900px
- ✅ 实现图片 base64 上传
- ✅ 添加编辑器生命周期管理
- ✅ 更新按钮文字（保存→提交）

### v1.0.0 (2026-01-12)
- ✅ 初始版本
- ✅ 基础 CRUD 功能
- ✅ 使用 Element Plus 表单组件

---

## 🎉 完成状态

- ✅ wangEditor 集成完成
- ✅ 表单简化完成
- ✅ 样式调整完成
- ✅ 无 linter 错误
- ✅ 可以立即使用

**当前状态**: ✅ 已完成，可以使用

---

## 📞 后续优化建议

1. **图片上传**: 实现服务器端图片上传接口
2. **视频上传**: 实现视频上传功能
3. **内容安全**: 添加 XSS 过滤和内容审核
4. **富文本预览**: 在列表页添加内容预览功能
5. **草稿保存**: 添加自动保存草稿功能
6. **字数统计**: 显示内容字数统计

---

**更新完成！** 🎊

现在您可以使用功能强大的富文本编辑器来管理思政热点内容了。

