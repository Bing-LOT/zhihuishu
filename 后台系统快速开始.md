# 后台管理系统 - 快速开始

## 🎯 目标完成

✅ 后台管理系统已成功配置为独立的入口点
✅ 支持通过 `https://dszk.fzu.edu.cn/admin/index.html` 访问
✅ 前后台系统共享代码，但入口分离

## 📁 文件清单

### 新增文件

1. **admin/index.html** - 后台系统的 HTML 入口
2. **src/admin.ts** - 后台系统的 JavaScript 入口
3. **后台管理系统部署说明.md** - 完整的部署文档
4. **admin/README.md** - admin 目录说明

### 已修改文件

- **vite.config.ts** - 已配置多页面应用支持（之前已存在）

## 🚀 快速测试

### 1. 开发环境测试

```bash
# 启动开发服务器
npm run dev
```

然后访问：
- 前台: http://localhost:5173/
- 后台: http://localhost:5173/admin/

### 2. 构建生产版本

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

然后访问：
- 前台: http://localhost:4173/
- 后台: http://localhost:4173/admin/index.html

## 📦 部署到生产环境

### 方法一：直接部署 dist 目录

1. 构建项目：
   ```bash
   npm run build
   ```

2. 将 `dist/` 目录的所有内容上传到服务器

3. 配置 Web 服务器（Nginx 示例）：

```nginx
server {
    listen 80;
    server_name dszk.fzu.edu.cn;

    root /var/www/dszk;  # 指向 dist 目录
    index index.html;

    # 前台路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后台路由
    location /admin {
        try_files $uri $uri/ /admin/index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 方法二：使用 CI/CD 自动部署

在 `.gitlab-ci.yml` 或 GitHub Actions 中配置：

```yaml
deploy:
  script:
    - npm install
    - npm run build
    - rsync -av dist/ user@server:/var/www/dszk/
```

## 🔍 验证部署

### 检查文件是否上传成功

确保服务器上存在以下文件：
```
/var/www/dszk/
├── index.html              ✓ 前台入口
├── admin/
│   └── index.html         ✓ 后台入口
├── assets/                ✓ JS/CSS 资源
│   ├── admin-*.js         ✓ 后台入口 JS
│   ├── main-*.js          ✓ 前台入口 JS
│   └── ...
└── images/                ✓ 图片资源
```

### 访问测试

1. **前台网站**: 
   - URL: `https://dszk.fzu.edu.cn/`
   - 应该显示前台首页

2. **后台管理系统**: 
   - URL: `https://dszk.fzu.edu.cn/admin/index.html`
   - 应该显示后台管理界面

3. **直接访问 /admin/**:
   - URL: `https://dszk.fzu.edu.cn/admin/`
   - 应该自动加载 `admin/index.html`

### 检查浏览器控制台

打开浏览器开发者工具（F12），确保：
- ✅ 没有 404 错误
- ✅ 没有 CORS 错误
- ✅ 所有静态资源正常加载

## 🎨 后台管理系统功能

访问后台后，你可以看到以下模块：

### 1. 资源管理
- 首页 Banner 配置
- 思政概览
  - "党建+课程思政"工作概览
  - 党员教师课程思政示范展播
- 习思想的伟大实践
  - 习思想融入成果
  - 习思想优秀视频展播
  - 习思想融入学科教学实践示范案例展播
  - 总书记的福建足迹
- 思政资源管理
- 特色专题
  - 优秀思政课堂
  - 一院一品
  - 红色文化资源地图

### 2. 题库管理
- 题目录入和管理
- 题库分类

### 3. 数据分析与统计
- 访问量统计
- 用户行为分析

### 4. AI 智能管理
- AI 助手配置
- 智能问答

### 5. 系统配置
- 系统参数设置
- 用户管理

## 🔐 安全建议

1. **启用 HTTPS**
   ```nginx
   server {
       listen 443 ssl;
       ssl_certificate /path/to/cert.pem;
       ssl_certificate_key /path/to/key.pem;
   }
   ```

2. **添加访问控制**（可选）
   ```nginx
   location /admin {
       # IP 白名单
       allow 192.168.1.0/24;
       deny all;
       
       try_files $uri $uri/ /admin/index.html;
   }
   ```

3. **配置 HTTP 安全头**
   ```nginx
   add_header X-Frame-Options "SAMEORIGIN";
   add_header X-Content-Type-Options "nosniff";
   add_header X-XSS-Protection "1; mode=block";
   ```

## 📊 构建产物分析

构建后生成：
- **前台入口**: `dist/index.html` + `dist/assets/main-*.js`
- **后台入口**: `dist/admin/index.html` + `dist/assets/admin-*.js`
- **共享资源**: 
  - Vue 框架: `dist/assets/vue-vendor-*.js` (~106 KB)
  - 通用样式: `dist/assets/style-*.css`
  - 页面组件: 按需加载

## 🐛 常见问题

### Q1: 访问 /admin/ 显示 404
**解决**: 检查 Nginx 配置中的 `try_files` 规则是否正确

### Q2: 页面刷新后出现 404
**解决**: 确保配置了单页应用的路由重写规则

### Q3: 静态资源 404
**解决**: 检查 Nginx 的 `root` 路径配置

### Q4: CSS 样式丢失
**解决**: 确保 `/assets/` 路径可访问，检查构建时的 base URL 配置

## 📞 技术支持

如遇问题，请：
1. 检查浏览器控制台的错误信息
2. 查看服务器的访问日志和错误日志
3. 参考 `后台管理系统部署说明.md` 中的详细配置

---

**项目**: 福州大学 - 大思政课数智化平台  
**版本**: 1.0.0  
**构建工具**: Vite 7.2.4 + Vue 3.5.24








