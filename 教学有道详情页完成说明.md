# 教学有道详情页完成说明

## 概述

根据Figma设计稿（node-id=566-3393），已完成教学有道详情页面的开发。

## 设计稿地址

https://www.figma.com/design/61VdOb1lIMdp25yHxAygzx/%E7%A6%8F%E5%B7%9E%E5%A4%A7%E5%AD%A6%E6%80%9D%E6%94%BF%E5%AE%98%E7%BD%91?node-id=566-3393&m=dev

## 实现功能

### 1. 页面结构

详情页面包含以下主要部分：

#### （1）顶部区域
- **Header**：复用 `AppHeader` 组件，包含logo、搜索框、后台管理入口等
- **Banner**：复用 `AppBanner` 组件，显示"教学有道"标题
- **Navigation**：复用 `AppNavigation` 组件，主导航菜单

#### （2）面包屑导航
- 显示当前页面位置：`您的位置：首页 >> 教学有道 >> 课程详情`
- 支持点击跳转到首页或列表页

#### （3）课程信息卡片
- **左侧**：课程封面图（540x304px）
- **右侧**：
  - 课程标题
  - 基本信息（学校、学科门类、教学类别）
  - 授课教师列表（显示最多2位教师）
  - 累计观看人数
- **角标**：国家示范标签（如果是国家示范课程）

#### （4）Tab切换区域
- **左侧菜单**：
  - 课程简介（默认选中）
  - 教学设计
  - 教学视频
- **右侧内容**：
  - 根据选中的tab显示对应内容
  - 课程简介和教学设计：富文本显示
  - 教学视频：视频播放器

#### （5）相关推荐
- 显示4个相关课程推荐
- 使用卡片布局，包含封面、标题、教师、浏览量等信息
- 支持点击跳转到对应课程详情

#### （6）底部区域
- **Footer**：复用 `AppFooter` 组件

### 2. 技术实现

#### 更新的文件

1. **src/types/teaching.d.ts**
   - 新增 `Teacher` 接口，定义教师信息结构
   - 扩展 `TeachingCase` 接口，添加详情页所需字段：
     - `schoolName`：学校名称
     - `categoryName`：学科门类
     - `caseTypeName`：教学类别名称
     - `teachers`：授课教师列表
     - `introduction`：课程简介
     - `designContent`：教学设计
     - `videoUrl`：教学视频URL

2. **src/api/teaching.ts**
   - 新增 `getRelatedCases` 方法，获取相关推荐课程

3. **src/views/Teaching/Detail.vue**
   - 完全重构详情页组件
   - 实现响应式布局
   - 实现Tab切换功能
   - 集成相关推荐功能
   - 添加模拟数据支持（API未就绪时使用）

#### 核心功能

- **数据加载**：
  - 页面加载时自动获取课程详情
  - 自动增加浏览量
  - 自动加载相关推荐课程
  
- **Tab切换**：
  - 使用响应式变量 `activeTab` 控制当前显示的内容
  - 左侧菜单高亮显示当前选中项
  - 右侧内容根据选中项动态切换

- **数据展示**：
  - 富文本内容使用 `v-html` 渲染
  - 视频使用HTML5 video标签播放
  - 浏览量格式化显示（超过1万显示为"x.x万人"）

- **交互功能**：
  - 面包屑导航支持点击跳转
  - 相关推荐卡片支持点击跳转
  - 鼠标悬停时卡片有动画效果

### 3. 样式实现

#### 设计还原

- **颜色**：
  - 主色红：`#c30d23`（品牌色）
  - 基础黑：`#333333`（文本色）
  - 背景灰：`#f9f9f9`（次级背景）
  - 白色：`#ffffff`（主背景）

- **字体**：
  - Source Han Sans CN（思源黑体）
  - Microsoft YaHei（微软雅黑）

- **尺寸**：
  - 页面最大宽度：1920px
  - 内容区宽度：1200px（左右各留360px边距）
  - 课程封面：540x304px
  - Tab切换区高度：720px

- **间距**：
  - 主要区块间距：40px
  - 卡片间距：24px
  - 组件内间距：16px

#### 响应式设计

- 页面宽度固定为1200px内容区
- 左右边距360px（基于1920px设计稿）
- 所有图片使用 `object-fit: cover` 保持比例

### 4. 路由配置

路由已在 `src/router/modules/public.ts` 中配置：

```typescript
{
  path: '/teaching/detail/:id',
  name: 'TeachingDetail',
  component: () => import('@/views/Teaching/Detail.vue'),
  meta: {
    title: '课程详情'
  }
}
```

## 访问方式

### 开发环境

1. 确保开发服务器正在运行：
   ```bash
   npm run dev
   ```

2. 访问详情页（以课程ID为1为例）：
   ```
   http://localhost:5173/teaching/detail/1
   ```

3. 或者从教学有道列表页点击任意课程卡片进入详情页

### 测试建议

1. **基本功能测试**：
   - 访问详情页，检查页面是否正常加载
   - 检查课程信息是否正确显示
   - 点击面包屑导航，测试跳转功能

2. **Tab切换测试**：
   - 点击左侧菜单项，检查Tab切换是否正常
   - 检查右侧内容是否正确显示
   - 检查选中状态样式是否正确

3. **相关推荐测试**：
   - 检查相关推荐卡片是否正确显示
   - 点击推荐卡片，测试跳转功能
   - 检查卡片悬停动画效果

4. **响应式测试**：
   - 检查各种浏览器下的显示效果
   - 检查布局是否与设计稿一致

## 数据接口

### 详情接口

```
GET /api/teaching/cases/:id
```

返回数据结构：
```typescript
{
  code: 200,
  data: {
    id: number | string,
    title: string,
    coverUrl: string,
    teacherName: string,
    viewCount: number,
    schoolName?: string,
    categoryName?: string,
    caseTypeName?: string,
    isNational?: boolean,
    teachers?: Array<{
      name: string,
      title?: string
    }>,
    introduction?: string,
    designContent?: string,
    videoUrl?: string
  }
}
```

### 相关推荐接口

```
GET /api/teaching/cases/:id/related?limit=4
```

返回数据结构：
```typescript
{
  code: 200,
  data: TeachingCase[]
}
```

## 模拟数据

当API未就绪时，页面会自动使用模拟数据，确保页面可以正常显示和测试。

模拟数据包括：
- 课程基本信息
- 课程简介（含富文本格式）
- 相关推荐列表（4条）

## 注意事项

1. **图片资源**：
   - 封面图片路径需要确保正确
   - 建议使用 `/images/` 开头的相对路径
   - 或使用完整的URL地址

2. **富文本内容**：
   - 课程简介和教学设计支持HTML格式
   - 建议使用 `<p>` 标签包裹段落
   - 可以使用 `style="text-indent: 32px;"` 实现首行缩进

3. **视频播放**：
   - 视频URL需要可访问
   - 支持常见的视频格式（mp4, webm等）
   - 如果没有视频URL，显示"暂无视频"提示

4. **浏览量统计**：
   - 访问详情页时自动调用增加浏览量接口
   - 浏览量格式化显示（超过1万显示为"x.x万人"）

## 下一步工作

1. **API对接**：
   - 等待后端接口开发完成
   - 测试真实数据的显示效果
   - 根据实际数据格式调整代码

2. **功能增强**：
   - 添加课程收藏功能
   - 添加评论功能
   - 添加分享功能

3. **性能优化**：
   - 图片懒加载
   - 视频预加载优化
   - 路由缓存优化

## 更新记录

- **2026-01-13**：完成教学有道详情页开发
  - 实现完整的页面布局
  - 实现Tab切换功能
  - 实现相关推荐功能
  - 添加模拟数据支持

